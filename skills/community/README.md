# Community Skills

该目录用于存放社区下载或用户共享的 Skill Capsule。

## 当前约定

- 单个技能包以 `.yaml` 文件形式存储。
- 第三方打包格式可使用 `.skill.zip`，安装后解压到本目录。
- 建议保留 `metadata.author` 和 `metadata.version` 以便兼容性检查。

## 文件命名建议

- 使用小写蛇形命名：`my_custom_skill.yaml`
- 避免与内置技能重名
