[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "blender-frontend-blender"
version = "0.1.0"
description = "BlenderAgentBot Blender panel UI - artist-friendly chat interface"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest", "ruff"]

[tool.pytest.ini_options]
testpaths = ["tests"]
